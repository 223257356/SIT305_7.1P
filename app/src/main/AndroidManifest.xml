<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.sit305_91p">

    <!-- Required Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!-- Include coarse location for better user experience on Android 12+ -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SIT305_71P"
        tools:targetApi="31">

        <!-- Google Maps API Key -->
        <!-- IMPORTANT: Replace YOUR_MAPS_API_KEY with your actual Maps API key -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyCTwentYgan_QVSReTH87YLEHOb6QJIpF8" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Declare CreateAdvertActivity -->
        <activity
            android:name=".CreateAdvertActivity"
            android:label="Create New Advert" />

        <!-- Declare ShowItemsActivity -->
        <activity
            android:name=".ShowItemsActivity"
            android:label="Lost &amp; Found Items" />

        <!-- Declare ItemDetailsActivity -->
        <activity
            android:name=".ItemDetailsActivity"
            android:label="Item Details" />

        <!-- Declare MapActivity -->
        <activity
            android:name=".MapActivity"
            android:label="Map of Items" />

    </application>

</manifest>
